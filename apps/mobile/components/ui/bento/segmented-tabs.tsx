/**
 * Legacy alias — re-exports the unified SegmentedControl with the
 * `BentoSegmentedTabs` name for callers that haven't been migrated yet.
 */
export { SegmentedControl as BentoSegmentedTabs } from "../segmented-control";
